{% extends 'baseAdmin.html.twig' %}

{% block title %}Hello TDAdminController!{% endblock %}

{% block body %}
    <h1>Video</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ video.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ video.title }}</td>
            </tr>
            <tr>
                <th>Lien</th>
                <td>{{ video.lien }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('t_d_video_index') }}">back to list</a>

    <a href="{{ path('t_d_video_edit', {'id': video.id}) }}">edit</a>

    {{ include('td_video/_delete_form.html.twig') }}
{% endblock %}
