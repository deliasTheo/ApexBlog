{% extends 'baseAdmin.html.twig' %}

{% block title %}Hello TDAdminController!{% endblock %}

{% block body %}
    <h1>Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('t_d_category_index') }}">back to list</a>

    <a href="{{ path('t_d_category_edit', {'id': category.id}) }}">edit</a>

    {{ include('td_category/_delete_form.html.twig') }}
{% endblock %}
