<form method="post" action="{{ path('t_d_world_record_solo_delete', {'id': world_record_solo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ world_record_solo.id) }}">
    <button class="btn">Delete</button>
</form>
