{% extends 'baseAdmin.html.twig' %}

{% block title %}Hello TDAdminController!{% endblock %}

{% block body %}
    <h1>Tag</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tag.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tag.name }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ tag.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('t_d_tag_index') }}">back to list</a>

    <a href="{{ path('t_d_tag_edit', {'id': tag.id}) }}">edit</a>

    {{ include('td_tag/_delete_form.html.twig') }}
{% endblock %}
